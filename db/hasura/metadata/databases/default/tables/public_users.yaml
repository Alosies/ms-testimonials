table:
  name: users
  schema: public
array_relationships:
  - name: identities
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: user_identities
          schema: public
