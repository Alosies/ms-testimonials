fragment FormQuestionBasic on form_questions {
  id
  step_id
  organization_id
  question_type_id
  question_key
  question_text
  placeholder
  help_text
  display_order
  is_required
  min_length
  max_length
  min_value
  max_value
  validation_pattern
  allowed_file_types
  max_file_size_kb
  is_active
  created_at
  updated_at
  question_type {
    id
    unique_name
    name
    category
    input_component
  }
}
