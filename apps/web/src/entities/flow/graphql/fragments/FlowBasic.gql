fragment FlowBasic on flows {
  id
  form_id
  organization_id
  name
  flow_type
  is_primary
  display_order
  created_at
  updated_at

  # Flattened branch columns (ADR-009 Phase 2)
  branch_question_id
  branch_field
  branch_operator
  branch_value
}
