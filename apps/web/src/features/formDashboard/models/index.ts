/**
 * Dashboard Models
 *
 * Re-exports types from API schemas for use in components.
 * All types come from @api/shared/schemas/dashboard.
 */

export type {
  Period,
  MetricSentiment,
  MetricWithBenchmark,
  SessionStats,
  FunnelStep,
  FunnelData,
  DeviceBreakdown,
  CountryEntry,
  AudienceData,
  RatingDistributionEntry,
  RatingData,
  BenchmarkMetadata,
  DataAvailability,
  DashboardResponse,
  DashboardErrorResponse,
} from '@api/shared/schemas/dashboard';
