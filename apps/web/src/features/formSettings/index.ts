/**
 * Form Settings Feature
 *
 * Provides UI and logic for configuring form settings including:
 * - General settings (name, product name, product description)
 * - Form status (draft/published)
 * - Design (primary color)
 * - Sharing (public URL)
 */

// UI Components
export { FormSettingsPanel } from './ui';

// Composables
export { useFormSettings } from './composables';
