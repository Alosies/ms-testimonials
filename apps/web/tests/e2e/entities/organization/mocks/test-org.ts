/**
 * Test Organization Mock
 *
 * Static organization data for tests that don't need real API calls.
 */
import type { TestOrganization } from '../types';

export const TEST_ORG_MOCK: TestOrganization = {
  id: 'test-org-id',
  slug: 'test-org',
};
